- [Optional-field default merge trap](default-merge-optional-field-trap.md) — `{...default, ...stored}` leaks new default-only fields onto every pre-existing saved row missing them; gate new-field defaults on "no saved row at all", not "field absent in saved row".
- [Email/SMS transport seam](email-transport-seam.md) — lib/email-transport.ts owns ALL sgMail.send + twilioClient.messages.create calls; dev suppression gate; guard test enforces the seam.
- [Universal pitch-slot exclusion list](pitch-slot-universal-exclusion.md) — pitch resolution gates on a 3-slug security exclusion list, not category; seam wins over caller-supplied pitch_block_html whenever userId resolves.
- [Customized email templates skip starter refresh](email-template-customized-skip.md) — starter_hash NULL rows are boot-skipped forever; fix stale copy via the admin editor PUT (versioned+audited), never seed edits.
- [Onboarding step migrations chain, don't replace](onboarding-six-step-migration-chain.md) — superseded step-contract migrations must be kept + run in historical order at boot, frozen to literal step numbers, never the live constant.
- [Lifecycle email image/token seams](lifecycle-email-image-and-token-seams.md) — one qualifyPublicAssetUrl seam for images; raw-{{token}} bug was test-script omission; wrapHtml footer carries a dark-mode !important/data-ogsb lock.
- [Grant-repair rank reuse bug](onboarding-repair-finder-launchpad-gap.md) — never reuse a partner-assignment (rank-floored) eligibility helper to compute variant-resolution rank; keep the two ranks separately computed.
- [Prod calls needing a secret](prod-ops-call-secret-workflow.md) — bash/code_execution have no secrets; call an OPS_API_KEY-gated prod endpoint via a throwaway console workflow (use ./…/node_modules/.bin/tsx — npx tsx can be missing there) so process.env is populated, then remove the workflow.
- [NMI refund/chargeback poller + partnered-cohort metric](nmi-refund-poller-cohort-metric.md) — read-only daily poller into member_refund_events; cohort degrades gracefully via a placeholder getPartneredMemberIds() until partner_assignments exists.
- [NMI_LIVE_MODE fail-closed money gate](nmi-live-mode-gate.md) — single seam in nmiPost() refuses all charge/refund/void/vault unless NMI_LIVE_MODE==="true"; query.php stays ungated; every test hitting the real (unmocked) gateway must set the flag or it 502s.
- [Billing dead-man's-switch + money limiter](billing-deadman-switch-and-limiter.md) — heartbeat/digest in Postgres+setInterval not Redis/BullMQ; stamp heartbeat in finally; digest reports true 24h run count; money limiters fail CLOSED; tests raise env limits.
- [KB content-campaign seed pattern](kb-content-campaign-seed-pattern.md) — per-root truth docs are authored as TS boot-seeds (the human gate), NOT pushed through the staging UI; fixed verified-at keeps them citable without resetting the aging clock.
- [KB answer-time privacy scrub](kb-answer-time-privacy-scrub.md) — email+phone+coach-name scrubbing runs at ingestion AND at retrieval time in chat/voice/rag-retriever; Robin pattern is Sheph?[ae]rd (4 variants); scrubKbDoc preserves null (not "").
- [Blitz caption dynamic linking](blitz-caption-dynamic-linking.md) — caption uploads link to EVERY lesson a video appears in, derived live from BLITZ_BODY_HTML (shared pkg) via clean Vidalytics id; sanitizeVidalyticsId recovers id from mangled filenames; store id not lesson-list.
- [Blitz guide + branded design](blitz-guide-generated.md) — live /blitz hand-maintained (regen off via ALLOW_BLITZ_REGEN); SPA nav must use wouter <Link>, courseId validator accepts blitz-hub-step-v2-{id} (blitz-spa-navigation.md).
- [Supported affiliate networks](affiliate-networks-supported.md) — only Media Mavens & ClickBank; MaxWeb/Affiliati removed by design. Don't "fix" the lingering maxweb label in dead LessonLibrary.tsx.
- [Blitz is sole progress tracker](blitz-sole-progress-tracker.md) — pre-launch: Training Library retired to admin-only reference; Blitz completion is the only progress signal; repoint health/churn scoring off legacy `progress` table; keep `blitz-hub-step-v2-N` ids when consolidating to DB.
- [Blitz curriculum single source](blitz-curriculum-shared.md) — 23-step skeleton owned by @workspace/blitz-curriculum; surface label maps stay local but MUST be drift-guarded vs BLITZ_SECTION_IDS; courseId prefix single-sourced into raw SQL too.
- [Monorepo typecheck stale cache](monorepo-typecheck-cache.md) — concurrent package typechecks corrupt the composite ref cache -> spurious "@workspace/db has no exported member"; rebuild libs with `tsc -b lib/db lib/api-zod lib/auth`, run typechecks sequentially.
- [Moderation pod-silent threshold](moderation-pod-silent-threshold.md) — pod staleness (2× rolling window, totalCount===0) now single-source in lib/moderation-shared; both card + alerter import it. CI gate lives in api-server tests only.
- [KB ingestion DB vs files](kb-ingestion-db-vs-files.md) — editing knowledge-base/*.txt does NOT update seeded knowledgebase_docs rows (ON CONFLICT title DO NOTHING); must UPDATE DB too + restart for system-prompt cache; title has UNIQUE constraint.
- [Prod super_admin bootstrap](prod-superadmin-bootstrap.md) — prod started with 0 super_admins (deadlock). Boot hook ensureFoundingSuperAdmins now ALWAYS enforces the hardcoded founders list (see FOUNDING_SUPER_ADMINS in code) as super_admin (idempotent; UI demotion reverts next deploy); must publish to take effect.
- [NMI checkout idempotency outcomeType](nmi-checkout-idempotency.md) — store outcomeType enum in every idempotency result; never infer success from status string alone or reconciliation-needed replays misclassify as declines.
- [Drizzle ANY(array) record-cast pitfall](drizzle-array-any-cast.md) — `ANY(${jsArray}::int[])` throws runtime 42846 "cannot cast record to integer[]"; pass a `{1,2,3}` literal string param instead. Typechecks fine, fails only at query time.
- [Express req spread loses headers](express-req-spread.md) — spreading an Express Request (`{ ...req, userId: x }`) creates a plain object; non-enumerable props (headers, ip) vanish. Use logAuditEvent directly with explicit actor fields instead of logAdminAction with a faked req.
- [Prod email/verification config](prod-email-verification-config.md) — prod verification/reset emails skip until SENDGRID_API_KEY + PORTAL_URL set AND a republish picks them up; agent cant republish/set-secrets/write-prod.
- [Machine brand-wiring republish hold](machine-brand-wiring-republish-hold.md) — 5-brand grant fix is merged-but-intentionally-not-live; don't republish until user signals ONE coordinated BTS+Machine deploy (post-Tapfiliate).
- [Drizzle push pre-existing drift](drizzle-push-drift.md) — full `db push` wants to drop unrelated sequence_* columns (data loss); add new columns via targeted SQL ALTER + matching schema field, never a force push.
- [Coach-name privacy filter](coach-name-privacy-filter.md) — assistant shows coach FIRST names only; system prompt reads qa-articles.txt/glossary.txt RAW (bypasses filter); coach surnames have inconsistent source spellings, so filter rules must tolerate variants.
- [E2E gotchas: proxy hang + shared-redis 429](e2e-loopback-proxy-hang.md) — login calls stall via vite proxy/Playwright request fixture (use direct fetch 127.0.0.1:8080 + inject cookie); reruns trip the shared-redis login limiter (e2e-shared-redis-rate-limit.md).
- [Auth email links need matching SPA routes](auth-email-link-routes.md) — every email-template path needs a portal `<Route>`+page or it 404s on the catch-all; reset = two pages (request `/forgot-password` + complete `/reset-password`).
- [Tapfiliate list pagination](tapfiliate-pagination.md) — `/programs/` paginates ~25/page via `?page=N`; loop+dedupe or page-2 programs (e.g. "Heat Haven") silently vanish. Base is api.tapfiliate.com/1.6.
- [Prod DB data fixes via startup hooks](prod-db-data-fixes-via-startup-hooks.md) — post-merge only hits dev; idempotent data repairs reach prod only by running on server boot. Watch the esbuild import.meta.url CLI-guard bundle trap. NOTE: schema DDL is forbidden here — see prod-schema-via-publish-flow.md.
- [Prod schema = Publish flow only](prod-schema-via-publish-flow.md) — never fix a failing prod migration with agent scripts/deploy hooks/startup DDL; make dev right + re-publish. unify-coaches diff dies on session_pack_coaches FK drop-order; prod data-safe (0 bookings).
- [Post-merge timeout: conditional push](post-merge-timeout-conditional-push.md) — never run drizzle push-force unconditionally in post-merge; gate it behind the drift test. Raising timeout alone won't fix it.
- [Bash background process lifetime](bash-background-process-lifetime.md) — nohup/& jobs from the bash tool die when the call returns; run long work synchronously per-call or as parallel blocking calls.
- [Long test runs via temp workflow](long-test-runs-via-temp-workflow.md) — suites over the bash 2-min cap (full portal vitest, test:e2e) run via a temp console workflow logging to /tmp with an EXIT_CODE marker, polled with sleep+tail.
- [Portal app intro videos](portal-app-intro-videos.md) — 7 videos+posters in portal/public; transcode rebrand HEVC 1080p exports to H.264 720p60 faststart before replacing; never commit raw exports.
- [Auth token 401 refresh](auth-token-refresh-data-layer.md) — 15-min access JWTs; customFetch single-flights 401→/api/auth/refresh→retry. "Reload fixes the page, in-app nav doesn't" = token expiry, not routing.
- [Verify static deploy serves current source](static-deploy-live-bundle-verify.md) — "published but still broken" on the static portal: prove live bundle = fixed code (getDeploymentInfo + git timeline + rebuild & match Vite content-hash) BEFORE chasing a code bug; usually browser cache.
- [Entitlements have no admin bypass](entitlements-no-admin-bypass.md) — entitlements are product-derived only; admins w/ no products get empty set. 3 gating layers (sidebar, EntitlementRoute, server guards) each need their own admin bypass; never grant via getUserEntitlements.
- [Coach = full member access](coach-member-access-bypass.md) — coach/admin get the whole membership menu via a per-gate role bypass, never via getUserEntitlements (keeps tier/commission math product-derived).
- [Share @workspace/db constant w/ browser](db-registry-browser-subpath.md) — db barrel is server-only (pg Pool); expose pure constants (e.g. ENTITLEMENT_KEYS) via a side-effect-free subpath export, never the barrel, in browser code.
- [Admin panel-api error shapes](admin-panel-api-error-shapes.md) — API emits TWO error shapes: route handlers `{error:"str"}` vs shared sendError/RBAC `{error:{code,message}}`; client must extract via helper or `new Error(obj)` renders "[object Object]" toast.
- [Sessions: rotation + member self-service](session-last-seen-rotation.md) — /auth/refresh rotates the session row (new row inherits createdAt); members manage own devices on /account (member-self-service-sessions.md; current-device needs the refresh cookie).
- [E2E one-command runner](e2e-one-command-runner.md) — `test:e2e` self-boots API(8080)+portal(25265) via Playwright webServer; keep ports in lockstep (config/vite proxy/auth.ts), run API NODE_ENV=development or login cookie drops; REDIS_URL inherited; `test:e2e:manual` skips auto-boot.
- [Live-chat support URL single source](live-chat-support-url-source.md) — TicketDesk default lives in @workspace/support-config; portal embed + backend health probe both import it (own env overrides). Never reintroduce a literal default.
- [Turnstile config & traps](turnstile-signup-captcha.md) — both secret + VITE site key needed, prod-only enforcement, republish required; junk secret blocks ALL prod logins (turnstile-fail-closed-trap.md); a global Secret shadows same-named .replit userenv (secret-overrides-userenv.md).
- [Community post approval gate](community-post-approval-gate.md) — posts created `active` for admins/good-standing authors (>=1 prior active post), else `pending`; standing ignores moderation flags by design (bans + async engine cover abuse).
- [community/dm storage matches schema](community-dm-storage-schema.md) — DM + community data layer is schema-consistent & typechecks clean; lingering api-server TS errors are the codebase-wide Express req.params string|string[] issue + test-helper drift, not data-layer.
- [Audit-log date-range boundaries](audit-log-date-range.md) — date-only end must expand to 23:59:59.999Z (else last day dropped); both read+export use parseAuditDateBoundary, UTC-evaluated.
- [api-server vitest DB-test hang](api-server-vitest-db-hang.md) — a killed vitest run mid-globalSetup leaves drizzle-kit push --force hung holding a lock; later DB tests hang forever. Clear it, run with SKIP_DEV_DB_SYNC=1 once schema is synced.
- [Community moderation flag dead-ends](community-moderation-flag-deadends.md) — pin/feature toggles dead if feed storage ignores the flag; reaction normalizers must map viewerHasReacted in BOTH post+comment (community-reaction-normalizer.md).
- [Session-credit ledger concurrency](session-credit-ledger-concurrency.md) — standalone 1-on-1 "session pack" credits: book+cancel share a per-member advisory lock; cancel claims via conditional status UPDATE...RETURNING + partial unique refund index; never read-then-write.
- [api-server schema-drift casts](api-server-schema-drift-casts.md) — vault.ts/seed-vault.ts/admin-communications.ts(sequences)/slot-engine.ts reference removed/renamed DB columns; only typecheck-clean via deliberate `as any` markers that preserve broken runtime. Don't trust these as working.
- [GHL calendar/booking auth](ghl-calendar-booking-auth.md) — raw GHL_API_KEY is dead (free-slots is public, don't trust it); real writes need agency-OAuth location token. RESOLVED: coach calendars live in sub-account JI6HzFwkNIr5VA2QUWUL (under Cherrington agency); book+Meet+cancel verified working.
- [Pack admin sessions stats filter](pack-admin-sessions-stats-filter.md) — /admin/coaching/sessions stats query must apply the SAME where+usersTable join as rows/count, else stats go global while the table is filtered.
- [Session-pack call vs calendar block](session-pack-call-buffer.md) — pack call=60min (member-facing endAt/durationMinutes) but GHL appointment blocks 90min (call+30 buffer); the GHL endTime is the only buffer lever in code.
- [Portal typecheck not in deploy build](portal-typecheck-net.md) — `vite build` ships with NO tsc check; the `typecheck` validation gate (portal+api-server, sequential) is the net, gates merges + in Project run button. Generated DashboardData lags backend (recentTools absent from OpenAPI spec).
- [integrations-openai-ai-react react devDeps + libs build in gate](orphaned-react-lib.md) — source-only react lib now adds react/@types/react as devDeps so `tsc --build`/`typecheck:libs` passes; typecheck gate already runs typecheck:libs + portal + api-server.
- [Slot engine honors per-window length/buffer](slot-engine-session-length-buffer.md) — recurring availability drives slot length + spacing from sessionDurationMinutes/bufferMinutes; overrides have no such cols (fallback 60/0); buffer also pads conflict checks.
- [Additive columns skip companion migration](additive-column-no-migration.md) — a new NOT NULL col WITH a DEFAULT needs only schema field + dev ALTER; post-merge push-force (gated on live-schema-drift test) adds it to prod cleanly. Companion .sql only for backfills/renames/constraints.
- [Table REMOVAL needs explicit post-merge DROP](schema-table-removal-post-merge-drop.md) — drift test only checks schema⊆DB, so push-force never fires for a removal; add idempotent DROP .sql AND a psql -f call in post-merge.sh or the table lingers in prod.
- [Per-category SMS prefs](per-category-sms-prefs.md) — ticket-reply SMS gated by ticketReplySmsOptIn in sendTicketReplyNotification (the SOLE enforcement point; queueSms is channel-generic, only re-checks master smsOptIn). Email always sends.
- [Vitest forks pool hangs in agent env](vitest-forks-pool-hang.md) — running vitest from bash hangs forever on the default `forks`/singleFork pool here; add `--pool=threads --no-file-parallelism` to run DB-backed suites.
- [TicketDesk delivery probes](ticketdesk-delivery-probes.md) — 3 health keys (widget/backlog/origin-gate); pending-orphan self-heal via idempotent createConversation + reconcile pass (ticketdesk-pending-orphan-selfheal.md).
- [Per-coach Google Drive OAuth](coach-google-oauth-drive.md) — coaches connect their OWN Drive via portal OAuth; callback MUST be public + signed state; ingest unions all Drives; Meet-API feasibility notes in meet-recording-ingest-feasibility.md.
- [Pack booking member leak](pack-booking-member-leak.md) — coachNotes/actionItems are coach-only; member book+reschedule must use MEMBER_BOOKING_COLUMNS projection, never raw `.returning()`, or they leak.
- [GHL coaching topic visibility](ghl-coaching-topic-visibility.md) — booking discussion-topic goes to the appointment's Internal Notes (POST /calendars/appointments/{id}/notes, fire-and-forget post-COMMIT, self-labeling body); title-preview + contact-note approaches were rejected.
- [Coaching attendance-targeted emails](coaching-attendance-targeting.md) — session-feedback targets any attendance row (fallback to entitlement audience when none); recording-ready targets registrants only; portal Coaching.tsx is static so nothing populates attendance yet.
- [Pack coaching recording ingest](coaching-recording-ingest.md) — Meet recording + Gemini notes/transcript auto-linked to a booking; COACH/ADMIN-ONLY (MEMBER_BOOKING_COLUMNS allow-list); Drive-pluggable via env, no-ops without creds.
- [1-on-1 coach roster boot-seeded](session-pack-coaches-roster.md) — real coaches (Sasha/Bruce/Michael/Todd) live in session_pack_coaches via an idempotent app.ts boot seed; legacy fakes (Sarah Mitchell/David Chen/Amara Williams) removed by exact name. Separate from old coachesTable group system.
- [Coaching-call reslot active-only](coaching-call-reslot-paused-gate.md) — template day/time edits re-slot future occurrences ONLY when active (consistent w/ top-up active-filter + pause-never-rewrites); don't drop the gate. Watermark must use desc().limit(1).
- [Prod SMS/Twilio config](prod-sms-twilio-config.md) — SMS disabled unless TWILIO_ACCOUNT_SID starts with "AC" (+matching token) AND a republish; construction only checks SID format, not auth; bash/sandbox don't get secrets, only workflows do.
- [Coaching calls return all occurrences](coaching-calls-recurring-occurrences.md) — /api/coaching-calls?upcoming returns EVERY future occurrence of recurring weekly_qa; weekly-cadence UI must dedup to one-per-slot (weekday+time+coachId, soonest) or rows look duplicated. No DB dupes.
- [Private Coaching rename](private-coaching-rename.md) — DONE (full rename, openapi→/coaching/private/*). EXCLUDE Concierge VA/KB/coaching:1on1:* keys. /coaches 500s on null bio unless route coalesces; generated private client is dead (frontend custom hooks hit /coaching/sessions/*).
- [comms-dedup string outcome](comms-dedup-string-outcome.md) — checkAndRecordSend returns "recorded"/"duplicate"/"error"; reserveSend proceeds only on "recorded"; a boolean mock silently suppresses ALL sends and fakes a passing test.
- [Shared CoachingCall schema](coachingcall-shared-schema.md) — CoachingCall zod schema is shared by dashboard + coaching-calls routes; a new REQUIRED field 500s every route that parses it; upgrade logic single-sourced in lib/coaching-upgrade.ts.
- [Session-pack recording deep link](session-pack-recording-deeplink.md) — 1-on-1 recording-ready uses dedicated `session_recording_ready` email+SMS deep-linking to /coaching/book-session?recording=<id>; group `recording_ready` untouched; SMS has no boot-refresh so new ensureRequiredSmsTemplates inserts-only.
- [Coach photo upload storage](coach-photo-upload.md) — coaches.photoUrl holds EITHER an absolute http(s) URL OR an internal `/objects/...` path; resolveCoachPhotoUrl (coaches-admin-api) must wrap every render (admin + member), backend parsePhotoUrl must accept the path.
- [Retell voice wiring](retell-voice-wiring.md) — voice feature is code-complete; needs 3 Retell secrets. RETELL_AGENT_ID MUST keep the `agent_` prefix or createWebCall 404s while the key looks fine. Also: interpretRetellSetupHealth only alarms on "misconfigured"; "not_configured"/"unknown"/benign skip ≠ failure.
- [Voice assistant naming/TTS](voice-assistant-naming-tts.md) — what the voice agent SAYS is set by buildVoiceSystemPrompt NAMING rules (spell numbers as words for TTS), not KB content; re-pushes on boot via drift, prod on publish.
- [Coach↔user link + Group Coaching soft-cancel](coach-user-link-group-coaching.md) — coaches map to users via NEW coaches.userId (seed-resolved from roster userEmail, never null-clobbered); admins on /coach/group-calls must NOT be scoped to a linked coachId; weekly-call soft-cancel (cancelledAt/By) is reversible + regen-safe.
- [Coach calendar-scope reconnect](coach-calendar-scope-reconnect.md) — pre-calendar-scope Drive-only Google grants need a reconnect; detect via stored scope, surface as needsCalendarReconnect, reuse the existing connect flow.
- [Cross-company coaching arbiter](coaching-cross-company-arbiter.md) — optional per-coach Conflict calendar mirrors BTS 1-on-1 bookings as busy blocks; conflict null = dormant; mirror block is best-effort (never roll back the appointment cancel/move) to avoid DB↔GHL divergence.
- [Coach optional fields coalesce](coach-optional-fields-coalesce.md) — coaches.specialties/bio are nullable; COACH_COLUMNS coalesces null->'' so the admin editor's .trim() never hits null. Coalesce any new optional coach text col there too.
- [Admin layout separate sidebar](admin-layout-separate-sidebar.md) — AdminLayout shares NO code with member Sidebar; scroll persistence needs h-screen + sessionStorage; member quirks in member-sidebar-scroll-persistence.md.
- [support-config stale dist](support-config-stale-dist.md) — portal/api typecheck "no exported member"/TS6305 from @workspace/support-config = stale composite dist; fix with `npx tsc -b lib/support-config --force`, not tsc --build.
- [user_products active-grant unique index](user-products-active-unique-index.md) — partial unique index = 1 active grant per (user,product); prod publish FAILS on existing dup actives (dedupe non-destructively first, never delete-to-satisfy); grant paths should catch 23505.
- [Coach callCalendars defensive read](coach-callcalendars-optional.md) — admin editor must read `(coach.callCalendars ?? [])`; server sends it but test mocks/legacy payloads omit it -> .find crash.
- [KB needs member terminology](kb-member-terminology-retrieval.md) — voice/KB search is lexical (websearch_to_tsquery); content must carry member aliases ("Mentee Master Agreement"), not just legal names, or natural queries miss. Anchor article + BTS_AGREEMENT_KB_TITLES.
- [coach_call_calendars call-type scope](coach-call-types-scope.md) — KNOWN_CALL_TYPES intentionally just private_coaching + one_on_one_va; onboarding is a planned FUTURE type, not a dropped requirement. Don't add unless asked.
- [Voice KB synonym layer](voice-kb-synonym-layer.md) — in-code alias map folds member phrasings -> canonical lexemes (refund) into the tsquery; DB integration test flaky (ts_rank title-weighted), unit-test the map.
- [TicketDesk two-way sync](ticketdesk-two-way-sync.md) — inbound (agent→member) via poller filtered to agent msg types; outbound (member→agent reply/resolve) must be pushed EXPLICITLY per action, gated on deliveryStatus='delivered', best-effort; member posts return chat_inbound so no echo.
- [Concierge live-row summary](concierge-submission-summary.md) — list payload lacks messages/attachments; fetch ticket detail per-row, parse intake body `Selected Task(s):` (labels) + attachments/`Uploaded Files (N)` for count; shared status labels in support-config.
- [KB taxonomy cols missing in fresh env](kb-taxonomy-cols-fresh-env.md) — doc_class/slug/home_root/node/ceiling/handoff/last_verified are schema-only (no companion .sql); a fresh dev DB lacks them, breaking seed-kb doc_class inserts. Land via ADD COLUMN IF NOT EXISTS, never push --force.
- [Surface-aware KB retrieval](surface-aware-kb-retrieval.md) — chat+voice share ONE path (lib/kb-retrieval.ts); curated>non-curated ranking, tag boost via {a,b}::text[] literal, nav-grounding fetches operations/navigation doc; never add voice synonyms triggering on password/live-coaching/commissions (test landmine).
- [Blitz decoupled from KB staging](blitz-reads-staging-table.md) — Blitz lessons now live in their OWN `blitz_lessons` table, NOT kb_staging_docs; removed from AI Document Review; data-safe copy-before-delete boot hook; coaching_call stays in staging.
- [Content Access Map backend](content-access-map-backend.md) — schema/registry/resolver/routes for admin-editable page-level access control; empty table = all pages open; empty slug array = delete row (never persisted).
- [Assistant Card Library KB unlink](assistant-card-library.md) — AI question generator intentionally severed from legacy knowledgebase_docs (dormant endpoint/columns); don't drop them or "fix" the missing Generate button; re-tie in phase 2.
- [authFetch double /api prefix](authfetch-double-api-prefix.md) — authFetch already prepends BASE_URL+api; a doubled /api 404 made the content-access query loop, flickering the guard spinner (the /blitz "twitch"); nav must fail-open on isError like the guard.
- [AI Knowledgebase phase-1 scaffold](ai-knowledgebase-phase1-scaffold.md) — NEW empty ai_live_documents table + /admin/ai-live-documents CRUD is NOT wired to retrieval; legacy knowledgebase_docs (sidebar "Knowledge Base" leaf) still feeds the AI. Naming inverts intuition. Migration = phase 2.
- [NMI renewal charger global batch](renewal-charger-global-batch.md) — processDueRenewals charges ALL due subs (not scoped); count-tests need a GLOBAL drain (orphans persist); double-run safety = advance + deterministic per-period key `sub_{id}_period_{periodEnd ISO}` (revert sub to test the key).
- [Transcript triage import](transcript-triage-import.md) — manifest keepers → Transcript Cleaner holding store; idempotency rides a provenanceNote marker (no schema col), partOrder stitch w/ single space, title from proposedTitle, confirm-gated.
- [Dunning retry idempotency keys](dunning-retry-idem-keys.md) — per-attempt key `sub_{id}_period_{periodEnd}_retry_{currentRetryCount}` where currentRetryCount is the value BEFORE the charge; cadence: +3d (count=1→attempt#2), +4d more (count=2→attempt#3=final).
- [AI Document Review categories](kb-document-review-categories.md) — KnowledgeBaseReview + staging GET/PATCH categorize by kb-staging schema enums (status/origin_type/authority_role/doc_class/home roots), NOT legacy `source`; no "reference" class; PATCH must persist taxonomy; runTriage includes pending_review.
- [Transcript Cleaner auto-naming grammar](transcript-cleaner-auto-naming.md) — titles assembled deterministically by call type from LLM building blocks (not free-form); date never fabricated; idempotent boot backfill re-titles cleaned holding docs.
- [Blitz innerHTML re-commit wipes filter](blitz-innerhtml-recommit-filter.md) — React silently re-applies dangerouslySetInnerHTML on async re-renders (content-access guard), wiping out-of-band DOM filtering w/o remount/effect-rerun; re-apply via MutationObserver.
- [Blitz caption filename autofill](blitz-caption-filename-autofill.md) — Transcript Cleaner auto-fills title/type/order/provenance from `blitz-lesson{NN}-{vv}-{slug}__{vidalyticsId}.ext`; parser is backend, naming is a contract — change regex + test in lockstep.
- [drizzle-zod generated column .omit()](drizzle-zod-generated-column-omit.md) — never list a generatedAlwaysAs col (e.g. searchVector tsvector) in createInsertSchema().omit(); drizzle-zod already drops it, zod v4 crash-loops "Unrecognized key" at boot (typecheck stays green).
- [ai_live_documents decoupled from legacy KB + drift regen trap](kb-live-docs-mirror-authority.md) — legacy→live mirror RETIRED (guard test bans re-coupling; corpus written only by pipeline/admin CRUD); never regen expected-drift.json against a broken/empty DB.
- [Core training → AI source corpus](kb-core-training-source-corpus.md) — 7 Pillars/Pillars-to-Blitz re-authored as seed text (can't import React pages); Blitz filed 1-doc-per-lesson (indexer truncates 9k); tag inventory ground truth + retired exclusions.
- [Blitz change-monitoring (dormant)](blitz-change-monitoring-dormant.md) — OFF-by-default "Scan for changes": detects changed core-training sources via content_hash, proposes revisions through the EXISTING supersede path; keep button disabled, no boot/schedule caller.
- [Partner staff role pattern](partner-staff-role-pattern.md) — mirrors coach role end-to-end EXCEPT never gets coach's member-content-access bypass; keep content-access product-derived only.
- [Transcript Cleaner flag contract + patch-refine](transcript-cleaner-flag-contract.md) — cleaner auto-corrects ALL proper nouns, emits exactly garbled_content+uncertain_authority; refine chat is patch-based (latency = output tokens); missing-date is titleNeedsInput, not a flag.
- [Onboarding step contract (now 6 steps)](onboarding-seven-step-contract.md) — Documents/ToS-signing step removed (browsewrap footer link instead); event-advanced steps only move via internal no-op-safe functions; renumbering needs a claim-row migration since old/new steps can collide in meaning.
- [Member-timezone formatter + route-guard forward-nav](member-timezone-and-onboarding-nav.md) — single shared formatter derives DST abbreviation via Intl (never hardcoded) from users.timezone; booking-confirmation "Continue" must forward-navigate to the member's actual current step, not just refresh auth.
- [KB tool-tag DB vocabulary](kb-tool-tags-db-vocab.md) — TOOL tags DB-managed, concept/troubleshooting stay code; read effective vocab via getEffectiveTag* at call-time (never a module const); protected caterpillar/grasshopper/crane.
- [Kickoff + partner call booking (GHL)](call-bookings-ghl.md) — round-robin kickoff coach, 5/day partner cap freed on cancel, pre-kickoff cutoff on first booking, idempotent step 4→5/5→6 advance.
- [Partner dashboard scoping + shared mark-done](partner-dashboard-scoping.md) — partner sees own roster only, admin needs ?partnerId=, admin is read-only even with partners:view; call-done MUST go through markPartnerCallDone (a future webhook-driven completion path must reuse it).
- [Per-row GHL location](ghl-per-row-location.md) — separate GHL sub-accounts per calendar-holder type; round-robin tests must isolate active rows in a shared dev DB.
- [Call duration from GHL calendar config](call-duration-from-calendar-config.md) — appointment length must read the calendar's `slotDuration`, never the free-slot grid spacing (a separate `slotInterval`); mock stacking (`mockImplementationOnce`) leaks between tests if call counts per test aren't tracked exactly.
- [Kickoff/partner call reminders](call-booking-reminders.md) — one SMS category covers both variants; per-member timezone formatter kept separate from group-coaching's fixed-zone one; dedup keyed per-booking.
- [Fleet-wide alerter test isolation](partner-escalation-fleet-wide-test-isolation.md) — unscoped table-wide evaluator tests need per-test (not per-file) fixture cleanup AND temporary exclusion of pre-existing real seeded rows, or counts are flaky.
- [Kickoff-coach tiering (LaunchPad vs full)](kickoff-coach-tiering.md) — tier column partitions round-robin pools; no-coach state must be a loud `setupPending` 200, never 404/empty/cross-tier fallback; seed never clobbers a real ghlCalendarId with a placeholder.
- [Onboarding tier resolver + variant step arrays](onboarding-tier-resolver-variants.md) — variant resolved+locked ONCE at creation from active product rank; none/launchpad/full step arrays deliberately duplicated frontend+backend (no shared package).
- [Onboarding tier-upgrade re-entry hook](onboarding-tier-upgrade-hook.md) — upgrade hook lives in insertUserProductGrant (single seam); tests granting products need onboarding_effects+sequence_enrollments cleanup before deleting users (FK).
- [Grandfather backfill dev-DB run](grandfather-backfill-dev-db-run.md) — testing a one-time marker-claim backfill in shared dev DB actually executes it forever; branch tests on pre-flight `alreadyMigrated`; watch for read-after-write lag on the dev DB proxy.
- [vitest spyOn on existing mock](vitest-spyon-on-existing-mockfn.md) — vi.spyOn on a property that's already a vi.mock() vi.fn() swaps identity; mockRestore() doesn't reliably undo it, orphaning stale top-level const refs elsewhere in the file. Read/mockClear() in place instead.
- [Soonest-first kickoff/partner assignment](soonest-first-partner-assignment.md) — merged earliest-first kickoff slot pool across tier coaches; partner assignment picks earliest-bookable-slot with ~3s never-block fallback to fewest-active on ANY probe error; assignment_method column records which.
- [Admin member hard-delete pipeline](admin-member-hard-delete.md) — GHL cancels first (own location, outside tx, abort on any failure); end active partner_assignments in-tx before delete for round-robin correctness; ANY financial row blocks.
- [Entitlement key exhaustive metadata map](entitlement-key-exhaustive-metadata.md) — a new ENTITLEMENT_KEYS entry needs a matching seed.ts ENTITLEMENT_METADATA row or `tsc` fails; adding the key alone compiles fine until you actually typecheck.
- [VIP pure-status product mechanics](vip-status-product-mechanics.md) — vip:status is rank-6 badge-only, always co-granted with 1year, both expiry clocks independent; excluded from mentorship/partner rank checks via one shared PARTNER_INELIGIBLE_SLUGS list.
- [Transcript Cleaner house-term normalization](transcript-cleaner-house-term-normalization.md) — closed BTS tool set (glossary-derived) gets aggressive near-miss auto-correct (Flexi→Flexy) via prompt tier + deterministic normalizeBtsHouseTerms backstop; equal-length-substitution guard protects member/ordinary words.
- [Vidalytics not plain-iframe embeddable](vidalytics-not-plain-iframe-embeddable.md) — Vidalytics needs its JS loader; direct embed base URL 403s. Use an internal static mp4 (portal public/videos) for anything needing a real `<iframe src>`.
- [Headless chromium missing libgbm in this env](headless-chromium-libgbm-workaround.md) — bundled Playwright chromium/chromium_headless_shell fails to launch (`libgbm.so.1` missing); launch with `executablePath` pointed at the nix-provided `chromium` binary (`which chromium`) + `--no-sandbox` instead.
- [Next-call panel source of truth](next-call-panel-source-of-truth.md) — persistent "next call" UI must query call_bookings directly (any type), never the partner-assignment endpoint, which is null for LaunchPad members by design; kickoff+partner can overlap.
- [Onboarding SMS/phone gate + revisit-save idempotency](onboarding-sms-phone-gate.md) — gate on master smsOptIn only (category flags are inert alone); step<current PATCH must be idempotent no-op, not a 400, or Back-then-save breaks.
- [KB value screener = recall-biased de-noiser](kb-value-screener-recall-biased.md) — keep-by-default front of KB pipeline; `error` is an isolated reliability status (not a verdict); dedup only near-identical WHOLE calls; NO PII/calibration here.
- [Publish + canary required close-out (split)](publish-and-canary-required-closeout.md) — any member-visible task is NOT done until published to prod AND the served bundle is canary-verified; task agents hand off a canary spec only (can't publish); main/merge flow publishes + runs accumulated canaries, batched OK.
- [Email pitch resolver seam](email-pitch-resolver.md) — {{pitch_block_html}} filled at send time in one seam (queueEmail/sendEmailNow); skip order matters; rank is DB-fresh, content is cached; isMachineMember is a stub.
- [KB cleaner format contract + glued-turn repair](kb-cleaner-format-contract.md) — own-line `Coach:` is canonical, only MID-LINE glued labels are drift; sweep finding 0 can be correct; exact_duplicate+0-exchange screenings are valid state.
- [KB review-gate analyzer lockstep](kb-review-gate-analyzer.md) — review-focus analyzer mirrors synthesis SOURCE CONFLICT prefix + [SITUATIONAL]-family tags locally; any synthesis marker/tag change must update kb-review-risk + tests together.
- [Group-call RSVP/join gating](group-call-rsvp-join-gating.md) — meet-link withholding duplicated in coaching list + dashboard upcomingCalls (keep in lockstep); join is a conditional UPDATE (never creates an RSVP), first joined_at wins.
- [gpt-5 reasoning tokens eat max_completion_tokens](gpt5-reasoning-token-budget.md) — tight budgets return 200 + empty content (finish=length), JSON.parse retry-loops to all-error while the backend looks healthy; give thousands of tokens headroom.
- [Coach archive vs delete](coach-archive-vs-delete.md) — history-holding coaches are archived, never deleted; reassign moves only upcoming calls + templates; every scheduling write must gate on coaches.isActive.
- [Email-blast forensics via SendGrid Activity API](email-blast-forensics-sendgrid-activity.md) — time-window the `/v3/messages` search to isolate one run from real production noise + prior failed attempts; a monkeypatched sgMail.send pre-flight image-404 gate catches broken asset URLs before firing for real.
- [Group-call RSVP morning-of reminders](group-call-rsvp-morning-reminders.md) — blanket coaching blast replaced by RSVP-driven 7AM-member-local reminders (prior-day RSVPs only, injectable now); coachingEmailOptIn seam + coaching-only unsubscribe; feedback/recording passes behind off-by-default env flags.
- [KB Navigation Docs + nav-gap flags](kb-navigation-docs-and-gaps.md) — `navigation` doc class rides existing staging pipeline (human gate absolute); (app,area) gap flags: sticky dismissal, auto-resolve on covering publish, verb/click-density detection gate; lucide `Map` shadows global Map.
- [KB truth navigation grounding](kb-truth-navigation-grounding.md) — truth drafts grounded in @workspace/portal-nav-map; NAVIGATION CONFLICT callouts for reviewers; boot-time drift scan flags docs when the nav registry changes.
- [Affiliate portal tracking seams](affiliate-portal-tracking-seams.md) — Tapfiliate conversion wiring: `tap_visit_id` (cookie-only) vs `tap_aff_id` (URL-only); portal-api /conversions takes EITHER; server-side tracking via nmiPost() hooks (replaces client-side `tapfiliate.js` callbacks for better 100% credit).
- [KB topic-index hardening](kb-topic-index-hardening.md) — gpt-5 @6000 tokens, honest per-source outcomes self-heal on force=false; node-agreement spot-checks measure run variance (gpt-5 self-agrees only ~67%); 429s need their own long backoff.
- [KB pipeline launch roadmap](kb-pipeline-launch-roadmap.md) — DECIDED: full pipeline runs in DEV only (synthesis hardening → full run → human review → one-time promotion of approved outputs to prod); prod never runs the pipeline.
- [KB Synthesis engine](kb-synthesis-engine.md) — per-node corpus consolidation; details in kb-synthesis-part2-state.md, kb-synthesis-full-source-read.md, kb-synthesis-hardening.md (loud LLM failures, no fallback, full corpus ≈13h).
- [KB Related-topics adjacency lockstep](kb-related-topics-adjacency.md) — NODE_NEIGHBORS feeds BOTH synthesis section and the mismatch flag; synthesis output must never self-flag; new taxonomy nodes need a neighbors entry.
- [Stale-nav sweep is manual-only](kb-stale-nav-sweep.md) — main runs it ONCE after merge in dev (console workflow, npx tsx …/sweep-stale-nav.ts); never boot/scheduled; append-only reviewer callouts, idempotent.
- [KB hybrid semantic retrieval](kb-semantic-hybrid-retrieval.md) — Replit AI proxy has NO /embeddings; direct OPENAI_API_KEY seam, graceful lexical-only dormancy, floor calibration pending key.
- [KB Analyze retrieval self-test](kb-analyze-retrieval-selftest.md) — triage self-tests drafts via real retrieveSurfaceAware; staging never stores embeddings; retrieval_gap non-blocking; synonym approvals are code-map markers; keep selftest SQL in lockstep with kb-retrieval.ts.
- [KB filed placement authoritative](kb-filed-placement-authoritative.md) — triage self-test/flags judge the FILED placement; AI taxonomy suggestions are advisory fallbacks + locked once filed; nav grounding orders navigation>overview (real nav map doc is 'overview').
- [possible_member_name flag vocab](kb-member-name-flag-vocab.md) — suppress false positives ONLY via exact-pair allowlists/stopwords (never structural rules); flags are computed live, so "removing" = extending analyzer vocabulary.
