Remove the ToS signing step from the member onboarding sequence. Decision: platform ToS moves to browsewrap (footer link) only; the mentorship agreement is signed upstream in GHL before portal access, so no in-portal signing step is needed. This is a sequence change, not a page-hide — the server-side gate must go with the page.

1. SEQUENCE: Remove the ToS/documents step from the onboarding flow. New sequence: 1 welcome video → 2 profile → 3 book kickoff → 4 book first partner call → 5 watch 7 Pillars → 6 first partner call completed → complete. Renumber everywhere the step order lives: STEP_ROUTES, the OnboardingLayout stepper ("Step X of 6" now), the guards' route arrays, and the PATCH /members/me/onboarding prerequisite validation — specifically REMOVE the signed-terms_of_service prerequisite gate entirely, and re-wire the remaining prerequisites (profile fields, kickoff booked, partner call booked/completed) to the new numbering.
2. EVENT ADVANCEMENT: The booking/webhook step-advance calls (kickoff booked, partner call booked, partner call completed) reference step numbers — update them to the new numbering. These are the event-advanced steps; a mismatch here strands members mid-sequence.
3. MID-FLIGHT MEMBERS: Anyone currently at old step 2 (ToS) or beyond maps to the nearest new step (old 1→1, old 2/3→2, old 4→3, old 5→4, old 6→5, old 7→6). Same transactional claim+remap pattern as the last migration. Members already onboardingComplete: untouched.
4. PRESERVE: The completion side effects (email-sequence cancel + nurture enrollment) — unchanged, just firing at the new final step. The Documents page component and the /documents/sign endpoint + signed_documents table STAY IN THE CODEBASE (do not delete — existing signature records are legal records, and the plumbing may be wanted later); only the onboarding step and its gate are removed.
5. FOOTER: Confirm the portal footer has a Terms of Service link to the current ToS content (add one if missing) — that's now the sole surfacing.
6. TEST: Update the onboarding suite — the full walk is now 6 steps, no signing prerequisite anywhere, a member who never signed anything completes onboarding, mid-flight mapping verified, completion side effects still fire.