Correction on batch item 2 (kickoff duration): your diagnosis (a) — "the GHL kickoff calendars are configured as 30-minute slots" — is FALSIFIED. Verified in the GHL UI (screenshot from our GHL admin): the kickoff calendars are set to meeting DURATION = 45 minutes, meeting INTERVAL = 30 minutes (interval = spacing between bookable start times), max bookings per day = 5. The 30 you found is the INTERVAL or a default in our chain, not the duration.

Re-diagnose and fix on our side:
1. Where did "30 minutes" enter our chain? Check in order: (a) does our getFreeSlots handling infer duration from slot spacing (interval) instead of reading the calendar's meeting duration? (b) does our createAppointment call pass an explicit endTime/duration — and if so, is it computed as start+30? (c) does call_bookings store a duration field, and what value did the walkthrough's kickoff booking (the July 3 9:00 AM row) get?
2. CRITICAL — inspect the actual GHL appointment created for that walkthrough booking (via the API, read-only): what are its real start and end times on the coach's calendar? If GHL shows 45 minutes, our bug is display/record-only. If GHL shows 30 minutes, our createAppointment is actively shortening appointments and every kickoff booked through the portal blocks the coach's calendar wrong — fix the createAppointment call to respect the calendar's configured duration (or omit end time and let GHL apply calendar config, if the API supports that).
3. Fix so that duration flows from the GHL calendar's configured meeting duration into the appointment, the call_bookings record, and the display — one source, no inference from interval, no hardcoded default. Kickoff will then show 45; partner calls (whose calendars are configured 30) will show 30 — both correct from the same mechanism, no special-casing.
4. Report: where the 30 came from, what the real GHL appointment duration was, and the before/after of the confirmation display.

Note for regression coverage: add a test that duration comes from calendar config and is never derived from slot interval — the interval (30) equaling the partner-call duration (30) is exactly the coincidence that let this slip.