TASK: Swipe Resource Bank — Surface + Foundation (Phase 1 of 2)

Schema: taxonomy (verticals → sub-verticals → angles) + swipe_bank_items (banner|advertorial, taxonomy FKs, title, source label, asset/thumbnail object paths, mime, size, sort, active) + an admin-editable site-copy slot for the disclaimer blocks. Additive DDL per conventions.
Gating, born enforced: swipe-bank page key in the registry; access-map seed with an explicit ownership branch — yse_swipe_resource_bank + the five tiers only; requirePageAccess on every API route including the content proxy; ContentAccessRoute + route + sidebar/nav-map in lockstep (the drift guards will demand it); admin/coach bypass wired per-gate.
Member surface at its own route, nav item "Swipe Resource Bank" (owners only): vertical tabs → sub-vertical sections; banners render as angle-grouped thumbnail grids; advertorials as preview cards with download. Lightbox: authFetch→blob for full-size (token-expiry-proof); grids on cookie-backed thumbs. Clean empty states per section so a sparsely-populated launch still looks intentional.
Content serving: bytes proxied through the authed gated route, Creative-Drive pattern exactly — no vault-style URL returns, no public paths.
Thumbnails at ingest: generate on upload (sharp or equivalent), store alongside originals; originals never modified.
Admin CRUD: manage taxonomy and items, single-file upload via the existing presigned-PUT flow, edit source labels/angles/sort, soft-disable items.
Disclaimer blocks: ported from the WordPress version but neutralized — old "The Cherrington Experience.com" site references become portal-neutral "we/this resource" language (no legal-entity naming needed) — rendered prominently on the gallery (top link + full block at bottom, matching the WP layout), admin-editable.
Taxonomy seed from the WordPress structure: Health (Diet/Weight Loss, Skincare/Anti-Aging, Blood Pressure, Diabetes, Muscle Building, E.D., Vision, Brain, Other), Wealth (Mortgage/Refinancing, Auto/Insurance, Casino/Gambling, Lottery, Business Opportunity, Bitcoin, Other), Everything Else — angles arrive with content.
Tests + acceptance: gating suite (no-product and FE-only-without-bank 403 on listing and content proxy; bank owner and tier member 200; admin bypass), nav visibility per ownership, drift guards green, disclaimer renders, thumbnail pipeline covered. Non-owners see no nav item and reach nothing by URL.