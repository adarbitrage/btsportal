Yes — draft and implement, with this exact scope:

1. THE FIX: Route POST /admin/members/:id/grant-product (admin-panel.ts:2236-2255) through insertUserProductGrant, replacing the raw db.insert. This picks up BOTH hooks at the seam: maybeForceOnboardingReentry (variant re-resolve + onboarding re-entry) and the partner round-robin assignment. Preserve the route's existing behavior otherwise (response shape, audit logging, permissions). ALSO: sweep admin-panel.ts (and any other route file) for OTHER raw db.insert(userProductsTable) call sites — the revoke path's counterpart, any bulk-grant, any create-with-product variant that might exist elsewhere. Every product-grant write in the codebase must go through insertUserProductGrant; report each site found and fixed. 

2. REGRESSION TEST — the exact sequence that caught this: create a member with NO products via the admin creation path (lands variant=none, complete=true), THEN grant launchpad via the admin grant ENDPOINT (the real route, not the helper directly), assert: onboardingComplete flips false, onboarding_variant becomes launchpad, step = first key of the launchpad array. Second test: same but grant 3month, additionally assert a partner_assignment row was created with assignment_method recorded. These pin the create-then-grant-via-admin sequence specifically — the gap all prior tests missed.

3. REPAIR SCRIPT for the two prod probes (launchpadtestmember@gmail.com, 3motestmember@gmail.com): idempotent, re-runs the hook logic against their existing grants (re-resolve variant, flip re-entry, fire partner assignment for the 3month member). Report-and-confirm pattern: state what it will change before executing. Do NOT auto-run against prod — it ships with the deploy and runs as a confirmed step. ALSO have it report (read-only) how many OTHER prod members have active backend grants but variant=none / no partner assignment — i.e., historical victims of this same bypass beyond the two probes — so the owner can decide whether the repair extends to them. Note: most historical members were grandfathered complete deliberately, so the repair must NEVER touch grandfathered=true rows' onboarding state — for those, only the missing partner assignments are in scope (and that's already the separate partner-backfill's job — reconcile with it, don't duplicate it).

4. After merge: confirm the ops route (routes/ops.ts:301) and NMI checkout still pass their existing hook tests untouched — this fix must not double-fire hooks on paths already wired.