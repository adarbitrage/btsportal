Read-only diagnostic — no changes. Requirements correction on onboarding: it must become TIERED. Free/front-end-only members get NO guided onboarding (straight to the normal Welcome page). LaunchPad gets a five-step variant (Welcome → Profile → Book Kickoff → 7 Pillars → complete) with kickoff bookings going ONLY to coach Neil. 3-Month+ gets the full six steps as built (kickoff round-robin across Todd/Mark/Bruce). Upgrades re-enter the flow. Map the following before the spec:

1. ENTRY GATING: The guard logic that routes members into onboarding (onboardingComplete checks). If a free/front-end member were flagged complete/exempt at creation, confirm their first login lands on the normal Welcome page. Where is the cheapest branch point to decide "does this member get a flow at all, and which variant" — and can it use the existing rank helpers (product-rank / getHighestProductLabel) to bucket free/front-end vs launchpad vs 3month+?

2. STEP CONTRACT VARIANTS: The sequence is one fixed six-step list (STEP_ROUTES, stepper, prerequisites, event-advance calls). What's the cleanest way to support per-tier step lists — LaunchPad's five (no Book Partner Call, no First Call) vs the full six — without forking the sequence? Do the event-advances (kickoff booked, partner booked, first call completed) reference absolute step numbers that break across variants? For LaunchPad, what completion trigger replaces "first partner call completed" — completing 7 Pillars, or explicit finish?

3. KICKOFF COACH TIERING: kickoff_coaches currently holds Todd/Mark/Bruce with no tier concept, and the kickoff round-robin draws from all active rows. What's needed to make kickoff coaching tier-aware: LaunchPad kickoffs → Neil ONLY; 3month+ kickoffs → Todd/Mark/Bruce round-robin? Note Neil currently exists ONLY as an accountability partner (partners table, calendar 2BCEaFdbunl34Idf4F56). Report: (a) can one person cleanly exist in both partners and kickoff_coaches, or does anything assume the sets are disjoint (dashboards, role checks, capacity math)? (b) if LaunchPad kickoffs booked onto Neil's EXISTING partner calendar: what breaks — the 6/day partner cap counting kickoffs, duration (his calendar is 30-min config, kickoffs are 45), the partner dashboard showing kickoff bookings? (c) confirm the cleaner path is a SEPARATE Neil LaunchPad-kickoff calendar (new GHL calendar, new kickoff_coaches row with a tier column) — what schema/logic changes that needs.

4. COMPLETION SIDE EFFECTS PER TIER: Completion currently cancels onboarding email sequences and enrolls nurture_frontend_to_upgrade. Report what fires and where, so the spec can set per-tier behavior: LaunchPad completing five steps (nurture_frontend_to_upgrade is presumably WRONG for them — they're backend already; what sequences exist for launchpad-to-3month nurture?), front-end members who never enter the flow (should their sequence handoff fire at account creation instead?), and 3month+ (unchanged).

5. UPGRADE RE-ENTRY: When a grant elevates tier (front-end→launchpad, front-end→3month, launchpad→3month), where in the grant path can "re-open onboarding at the right variant" trigger — same hook point as the partner round-robin? What onboarding state do upgraded members have (complete), and can the flow re-open for ONLY the missing steps — specifically the launchpad→3month case, where kickoff is already done and only the partner steps (assignment + Book Partner Call + First Call) remain? Do the guards handle onboardingComplete flipping back to false mid-membership, or does anything cache it?

6. EXISTING MEMBERS: Count in dev (and give the prod query for the runbook): members by tier bucket (free/front-end-only, launchpad, 3month+) crossed with onboarding state (not started / mid-flight / complete) — the deploy backfill must set every existing member correctly: free/front-end → exempt/complete; existing launchpad/3month+ who predate the flow → decide complete-as-grandfathered vs enter-the-flow (report counts so the owner can decide).

Report with file:line evidence. No edits — the tiered-onboarding spec follows this.