Add the VIP tier — a status product composed with standard mentorship grants. Rides BEHIND the admin grant-hook fix (VIP's only sales path is the admin grant path — do not build this until that fix is merged).

1. PRODUCT: slug `vip`, label "VIP", rank 6 in PRODUCT_RANK (above lifetime; sync the label map and the documented portal duplicate), type backend, 730-day term, entitlement keys: vip:status ONLY. No content keys — mentorship access comes from the separately-granted 1year/lifetime product. Boot-seeded like the other products, idempotent.
2. SALE MECHANICS (documentation + test, not new code): a VIP sale = admin grants `vip` + `1year` in one sitting via the member detail Products tab. The $20K lifetime upsell = grant `lifetime`. All through insertUserProductGrant (the fixed seam). Union handles access; expiry sweeps handle the two clocks independently — vip expires at 730 days (status/badge ends), 1year expires at 365 (mentorship + partner end, AS DESIGNED — do not extend anything on the vip term).
3. DISPLAYS: Level column shows "VIP" (rank 6 wins) — automatic via the label map. Member detail's product list already shows both grants with expirations — verify, no change expected. Partner dashboard: badge VIP mentees, showing the mentorship term alongside ("VIP · expires {date}" / "VIP · Lifetime") so partners know the client type on every call.
4. ACCESS MATRIX: `vip` joins as the 12th column in the content-access admin matrix. Copy-upward: vip inherits the ladder as the top rung. Note for the admin: since vip carries no content keys itself, checking vip boxes in the matrix is how you'd ever gate anything VIP-specific — none exists today, all boxes start unchecked.
5. TESTS: (a) admin-granting vip + 1year in sequence fires the upgrade hook and partner assignment exactly ONCE each (the double-grant idempotency case); (b) a vip+1year member's level reads VIP, onboarding variant resolves 'full', six-step flow entered; (c) simulated 1year expiry with vip still active: mentorship entitlements and partner assignment end, vip:status and the VIP badge persist to day 730; (d) granting lifetime to an existing vip+1year member elevates content access with NO onboarding re-entry (rank 5→6 is not a variant change) and no duplicate partner assignment.