Soonest-availability-first selection — the final booking-logic change. Two seams, one principle: a member is never paired with or shown someone who can't see them soon. Dispatch AFTER TB2 merges (shared assignment seam with TB1's hook — rebase carefully if TB2 is still in flight).

1. KICKOFF: MERGED-POOL SLOT OFFERING. The Book Kickoff step stops pre-selecting one coach. Instead: fetch getFreeSlots for EVERY active kickoff coach in the member's tier (full tier: Todd/Mark/Bruce; launchpad: Neil — pool of one, behavior unchanged), merge and sort earliest-first, render as one grid; each slot carries its owning coach, and booking creates the appointment + call_bookings row against that coach. The coach reveal (photo/bio) shows AFTER slot selection or on the confirmation — the member picks a TIME, the system supplies the human. Per-coach fetch failures degrade gracefully: merge whatever pools respond; only if ALL fail does the step show the loud "scheduling is being set up" state (never a silent empty grid). The tier filter from TA2 is untouched — merging happens WITHIN the tier pool only.

2. PARTNER ASSIGNMENT: SOONEST-FIRST WITH NEVER-BLOCK. Replace fewest-active as the primary rule in assignRoundRobin: for each active partner with a calendar ID, fetch next-7-days getFreeSlots; assign the partner with the EARLIEST bookable slot; fewest-active-assignments is the TIEBREAKER (same earliest day → lighter book wins). Two mandatory behaviors:
   (a) NEVER-BLOCK (blessed): the GHL availability check runs with a tight timeout (~3s total budget across candidates); on timeout, error, or GHL unreachable, fall back SILENTLY to the existing fewest-active rule, log the degradation (assignment_method: 'soonest' | 'fallback_fewest_active' recorded on the assignment row or its history), and never delay or fail the grant. Assignment must complete even if GHL is fully down.
   (b) CAPACITY SIGNAL: if every partner's earliest slot exceeds 7 days out, still assign the soonest one, but fire the capacity alert through the on-call dispatcher with the soonest-date attached ("Partner capacity: earliest available first-call is {date}") — throttled per the dispatcher's existing semantics so a busy week doesn't spam.
   Partners with no calendar ID (Myco until armed) are excluded from soonest evaluation exactly as they're excluded from round-robin today.

3. CAP INTERACTION: verify (and add a test) that a partner at their 6/day cap on a given day exposes no slots for that day through this path — soonest-first must naturally skip capped days. The cap logic from T5 should already enforce this at slot-offering; confirm it holds at assignment-time evaluation too.

TESTS: kickoff grid merges three coaches' mocked pools earliest-first and books the slot's owner; one coach's fetch failing still yields the other two's slots; partner assignment picks the earlier-slot partner over the lighter-book partner; the tiebreaker resolves same-day ties by fewest-active; GHL timeout at assignment yields a fewest-active assignment with the fallback method recorded, completing within the grant flow without error; the >7-day case assigns AND fires the alert once.
