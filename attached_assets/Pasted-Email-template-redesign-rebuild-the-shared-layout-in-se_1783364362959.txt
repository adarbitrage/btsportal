Email template redesign — rebuild the shared layout in sendBrandedEmail() (the confirmed single seam; every member-facing send inherits this). Design per the owner's brief:

1. LAYOUT: White background; the BTS logo blue as the accent (pull the exact hex from the logo/portal theme). Header: the BTS logo image (absolute URL to the hosted asset — email clients need full URLs) for lifecycle sends; for brand-substituted nurture sends, the header renders the resolved brand's marked wordmark as styled text (only BTS has a logo asset; front-end brands are text marks — the seam already knows the brand at render time). App-consistent typography and button styling (match the portal's primary button: the blue, rounded, white text). Email-client-safe HTML (tables/inline styles where needed — this must render in Gmail/Outlook/Apple Mail, not just browsers).
2. FOOTER on every email: the portal's footer links (Terms of Service link per the browsewrap decision, support → TicketDesk) + the site's full disclaimer text (the NO GUARANTEE... block from the portal footer) in small gray type.
3. PERSON-BLOCK SLOT: an optional card slot — photo (absolute URL), name, call type + datetime (member timezone, labeled), and bio when present (null-graceful, same rules as the portal cards). Wire it into the sends where a booking is the subject: kickoff + partner confirmations, both 24h reminders, reschedule/cancel notices. Same visual language as the portal's one-card-per-call pattern.
4. PITCH SLOT: an optional footer-adjacent slot (small heading + one line + one button) that the send sites can populate — build the SLOT in this task; the resolver that fills it is a separate task. Renders nothing when empty.
5. Ops sends (escalations, digest, TicketDesk fallback) stay plain — do not route them through the new layout.
6. VERIFY: render every member-facing send through the new layout in a preview harness; send at least the two booking confirmations + one reminder to a real inbox and screenshot the received result (Gmail at minimum). Canary spec in the completion report per the amended mechanic; the merge-flow publish carries it live.
