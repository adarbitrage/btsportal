Batch: onboarding fixes from live walkthrough + the approved ToS-step removal. Four items — implement in this order, because item 4 renumbers the sequence and items 1–3 must land against the final numbering.

1. DEAD CONTINUE BUTTON (root-caused, fix both confirmations): On the step-4 kickoff confirmation (BookKickoff.tsx ~322-331) and the step-5 partner-call confirmation (same template), the Continue handler gates on a local autoAdvanced state flag that never flips when the server already advanced the step at booking time (the normal case — the booking flow advances server-side, so the confirmation's own advance call no-ops and the flag stays false). Fix: remove the local-flag gate entirely. Continue is always enabled; on click, refetch the member's current onboarding step from the server and navigate to wherever the server says they are. Server state drives navigation — no client-side advance bookkeeping. Also remove the now-pointless useEffect advance call if the booking flow reliably advances (verify it does for both kickoff and partner bookings — the diagnostic confirmed kickoff).

2. KICKOFF DURATION — diagnose source, then fix: Confirmation shows "30 minutes"; kickoff calls are 45. Determine whether (a) the three GHL kickoff calendars (Todd Nx8nzFJxkxHQlQyx5ZSW, Mark wvSF5RfAi8FlsgHRo8IQ, Bruce wLvil3ING3i1d4oX7vg5) are configured as 30-minute slots in GHL — if so, flag for GHL-side reconfiguration (do NOT change GHL from code) and make our display render the appointment's actual duration; or (b) our code hardcodes/defaults 30 — if so, fix to read the real duration from the booking. call_bookings and the display must agree with the actual GHL appointment. Partner calls are 30 minutes BY DESIGN — do not touch partner durations.

3. TIMEZONE ON ALL CALL TIMES: Times render with no zone ("9:00 AM"). Fix: one shared formatter rendering in the MEMBER'S timezone with a derived abbreviation (e.g. "9:00 AM CDT" — derive from date for DST; never hardcode). Zone source: the member's profile timezone from the onboarding profile step, falling back to browser zone. Confirm scheduled_at is stored UTC. Apply the formatter to: kickoff confirmation, partner-call confirmation, call-day banner, partner dashboard (today view + mentee detail), and the member's partner panel.

4. REMOVE THE ToS STEP (approved decision — platform ToS moves to footer browsewrap; the mentorship agreement is signed upstream in GHL): Remove the Documents/ToS step from the sequence. New order: 1 Welcome → 2 Profile → 3 Book Kickoff → 4 Book Partner Call → 5 7 Pillars → 6 First Call → complete ("Step X of 6"). Renumber: STEP_ROUTES, stepper labels, guards, PATCH prerequisite validation (REMOVE the signed-terms_of_service gate; rewire remaining prerequisites to new numbers), and the event-advance calls in the booking/webhook flows (kickoff booked → advance to 4, partner booked → 5, first call completed → complete — per the new numbering). Mid-flight members: transactional remap old→new (1→1, 2/3→2, 4→3, 5→4, 6→5, 7→6); onboardingComplete members untouched. PRESERVE: completion side effects (email-sequence cancel + nurture enrollment) firing at the new final step; the Documents page component, /documents/sign endpoint, and signed_documents table STAY (existing rows are legal records) — only the step and its gate are removed. Confirm the portal footer links to the ToS (add if missing). Update the new-copy on both confirmations while in there: "You're all set — click Continue below to keep going."

TESTS: onboarding suite updated to the 6-step walk with no signing prerequisite; a regression test for item 1 — confirmation rendered with server already on the next step, Continue navigates correctly; mid-flight remap verified; completion side effects still fire; the shared time formatter unit-tested across DST boundary dates.