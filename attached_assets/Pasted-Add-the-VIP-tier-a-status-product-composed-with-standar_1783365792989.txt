Add the VIP tier — a status product composed with standard mentorship grants. The spec: VIP is a done-for-you managed service (2-year term) sold ONLY by the sales team via admin grant; portal access comes from a separately granted mentorship product (1year standard; the $20K upsell = grant lifetime). Two independent clocks BY DESIGN.

1. PRODUCT: slug `vip`, label "VIP", rank 6 in PRODUCT_RANK (above lifetime — sync the label map and the documented portal duplicate; PRODUCT_RANK remains the single tier authority), type backend, 730-day term, entitlement keys: vip:status ONLY. No content keys — mentorship access comes entirely from the separately-granted 1year/lifetime product. Boot-seeded idempotently like the other products.

2. SALE MECHANICS (test + docs, minimal new code): a VIP sale = admin grants `vip` + `1year` in one sitting via the member-detail Products tab — both grants flow through insertUserProductGrant (the fixed seam; the raw-insert bypass is dead, do not reintroduce any direct write). The $20K lifetime upsell = grant `lifetime` later. Expiry sweeps handle the clocks independently: 1year expiry at 365d ends mentorship content AND the partner assignment (AS DESIGNED — only ~5-10% of VIPs engage the mentorship; do NOT extend anything onto the vip term); vip expiry at 730d ends the status/badge.

3. ONBOARDING/PARTNER INHERITANCE (verify, not build): a vip+1year member resolves onboarding variant 'full' via the existing rank≥2 logic and enters the six-step flow; the partner round-robin fires on the 1year grant as it does today. VIP itself (rank 6) adds nothing here — confirm resolveOnboardingVariant and the partner-eligibility check treat rank 6 correctly (PARTNER_INELIGIBLE handling: vip alone with no mentorship grant should NOT trigger onboarding or a partner — it has no content and no flow; only the mentorship grant does).

4. DISPLAYS: Level column reads "VIP" (rank 6 wins — automatic via the label map). Member detail's product list shows both grants with expirations — verify. Partner dashboard: badge VIP mentees with the mentorship term alongside ("VIP · expires {date}" / "VIP · Lifetime") so partners know the client type on every call.

5. ACCESS MATRIX: `vip` joins as the 12th column, top of the copy-upward ladder. It carries no content keys, so all boxes start unchecked — note in the matrix UI help that checking vip boxes is how any future VIP-only content would gate.

6. TESTS: (a) admin-granting vip + 1year in sequence fires the upgrade hook and partner assignment exactly ONCE each (the double-grant idempotency case — this is its first real exercise); (b) the member's level reads VIP, variant resolves 'full', six-step flow entered; (c) simulated 1year expiry with vip still active: mentorship entitlements + partner assignment end, vip:status + badge persist to day 730 — THIS TEST ENCODES THE TWO-CLOCK DESIGN PERMANENTLY; (d) granting lifetime to a vip+1year member elevates content with NO onboarding re-entry (rank 5→6 is not a variant change) and no duplicate partner assignment; (e) a vip-ONLY grant (no mentorship) creates no onboarding entry and no partner assignment.

7. Per the amended mechanic: canary spec in the completion report (e.g. "VIP" present in the admin Level badge map in the served bundle; the vip product row seeded in prod on boot); merge-flow publish carries it.
