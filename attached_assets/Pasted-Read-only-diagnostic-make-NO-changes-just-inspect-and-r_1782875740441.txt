Read-only diagnostic — make NO changes, just inspect and report, for the BTS PORTAL app. Goal: I want to remove the member- and team-facing TICKET/SUPPORT UI from the BTS portal so mentees and staff aren't confused, because all ticketing lives in a separate app (TicketDesk, at tickets.buildtestscale.com). But I must NOT break the integration that FORWARDS support/voice/chat data to TicketDesk. Map both before I remove anything.

1. Ticket/support UI surfaces IN the BTS portal: List every page, route, and nav/sidebar item in the BTS portal related to tickets or support — e.g. /admin/tickets ("Ticket Queue"), Routing Rules, Canned Responses, Agent Performance, Support Analytics, any member-facing "Support"/"Contact"/"Submit a ticket" links, and any "Support"/"Tickets" nav entries (member nav AND admin nav). For each: the route, the component file, and the nav definition location. Distinguish member-facing from admin-facing.

2. The "Support Management / Admin Panel" surface: The admin panel at /admin/tickets is branded "ADMIN PANEL — Support Management" (Ticket Queue, Routing Rules, Canned Responses, Agent Performance, Resource Vault, Collections). Confirm plainly: is this admin panel part of the BTS PORTAL codebase, or is it TicketDesk's own admin being served/embedded/linked at a BTS URL? This determines whether it's mine to remove or is actually the external tool. Show where it's defined/registered.

3. The TicketDesk INTEGRATION (do NOT remove — I need to know what it is so it's protected): The screenshot shows BTS portal POSTing to https://tickets.buildtestscale.com/api/chat/session (one shows "DELIVERY FAILED"). List every place the BTS portal SENDS data to TicketDesk — support ticket forwarding, voice-call escalation, chat session creation, the "backfill"/delivery-tracking/"Delivery Skipped"/"Retry" mechanism. For each: the endpoint it calls, the file, and what triggers it. I want these preserved; I'm only removing the in-portal UI, not the forwarding.

4. Coupling check — the critical question: If I remove the ticket/support UI and nav from the BTS portal, does any of the TicketDesk-forwarding integration (#3) depend on those pages/components/routes existing? i.e. is the forwarding logic wired through the admin ticket UI, or is it independent (webhooks/services/background jobs) that would keep working if the UI is deleted? Flag anything where removing the UI would also break the forwarding to TicketDesk.

5. Member-facing entry points: Separately from admin, is there anything a MENTEE sees in the BTS portal about tickets/support — a "Support" sidebar item, a contact/help widget, a "submit a request" form, the chat widget in the corner? List each with its file/nav location, since those are the highest priority to remove for member clarity.

Report all of it with file + line evidence, clearly separating "in-portal UI to remove" from "TicketDesk integration to preserve." Make NO changes and delete nothing — I need the full map first so removal doesn't sever the connection to TicketDesk.
