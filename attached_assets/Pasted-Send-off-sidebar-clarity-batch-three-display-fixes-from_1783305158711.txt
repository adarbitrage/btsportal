Send-off / sidebar clarity batch — three display fixes from the 1yr walkthrough lane.

1. REDESIGN THE SIDEBAR NEXT-CALL PANEL — one card per call: The panel currently mixes one person's photo with both relationships' text (kickoff coach's photo above "Your Accountability Partner: {other person}" above one call's datetime — confusing). Redesign: render ONE card PER upcoming booked call, chronological: [person's photo] [call type + name: "Kickoff Call with Todd" / "Accountability Call with John"] [datetime, member timezone, labeled] [Join call link, emphasized on call day]. All upcoming calls shown (this member has two). Below the call cards, an optional single context line "Your accountability partner: John" only if an active assignment exists AND no partner call is among the cards (avoid redundancy). Populated-or-hidden unchanged. Screenshot on the 1yr probe (two calls booked) and the LaunchPad probe (one kickoff only).

2. PARTNER REVEAL HEADLINE + DESCRIPTION on the Book Partner Call step: Replace "Free 30-minute accountability call" (also stale copy — should already read "Your first...") with a proper introduction. Headline: "Meet Your Accountability Partner". Under the photo/name: "{Name} is your personal accountability partner — they'll walk with you through the entire program, checking in on your progress and keeping you moving. Book your first 30-minute call to meet them and set your pace together." (Adjust to fit the card, keep the substance: personal, whole-term, checks in, keeps you on track, first call sets cadence.) Bio renders below when present, as built.

3. SEND-OFF RECAP CARDS get faces: The "Your Booked Calls" cards on the send-off currently render text-only (coachName/date/time — no photo in the response by design). Add the person's photo to each call card: extend the send-off response with the coach/partner photo_url per call, render a small avatar per card, null-graceful. This matches the one-card-per-call sidebar pattern — same visual language in both places.