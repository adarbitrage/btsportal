Verify the NewsBreak purge stuck everywhere (dev + prod)

A previous task removed every mention of the confidential traffic-source name "NewsBreak" (internal codename: Caterpillar) from the entire project. It has since been merged into main and the app has been published to production. Your job is read-only verification — do NOT change any code or data. Check all of the following with case-insensitive searches for "newsbreak" (also try "news break" with a space):

Codebase and files: Run a case-insensitive search across the whole project (excluding .git and node_modules). Expected: zero matches anywhere — including the Blitz seed data in the admin knowledgebase pipeline route, blitz-seed.json, the portal's blitz-archive-lessons.json, the transcript cleaner's term lists, the nav-vocabulary ignore list, and the attached_assets folder.
Old transcripts deleted: Confirm artifacts/api-server/src/knowledge-base/coaching-transcripts.txt and the entire coaching-transcripts-split/ directory no longer exist, and that no code references them anymore (legacy KB seeding and the KB source registry rescan should have no trace).
Development database: Search every text column of every table in the public schema for the name. Pay special attention to: kb_staging_docs (both content and edited_content), kb_staging_archive, ai_source_documents, ai_live_documents, kb_source_node_extracts, kb_source_node_links, blitz_lessons (content and edited_content), knowledgebase_docs, transcript_cleaner_documents, kb_screened_exchanges, and content_gap_questions. Expected: zero rows.
Production database (read-only queries only): Run the same searches against prod. The three rows that were contaminated before the fix were: blitz_lessons id 50, ai_source_documents id 90, and knowledgebase_docs id 60 (that last one was member-facing and citable — it matters most). Confirm all three are now clean and that no other row in any prod table contains the name. Also confirm the boot-time repair logged that it ran (or that it now no-ops because everything is clean).
Member-facing surfaces: Confirm the published Blitz lesson "What's Working Now — Caterpillar Round 1 Recommendations" renders with the cleaned wording (it should say "Caterpillar" only), and that the AI Assistant's knowledge retrieval cannot surface the name.
Report back with a table: each location checked, the search used, and the result. If ANY mention remains anywhere, list exactly where it is (file path or table + row id, dev or prod) and stop — do not fix it without asking.