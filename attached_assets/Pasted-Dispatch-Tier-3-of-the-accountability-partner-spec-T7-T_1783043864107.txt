Dispatch Tier 3 of the accountability-partner spec — T7, T8, T9 — with the updates below, which supersede the spec where they conflict. T8 and T9 can run in parallel; T7 should land last or be carefully rebased, because it touches the onboarding step-advance that a live walkthrough is currently exercising.

CONTEXT CHANGES since the spec was written (all merged):
- The onboarding sequence is now SIX steps (ToS step removed): 1 Welcome → 2 Profile → 3 Book Kickoff → 4 Book Partner Call → 5 7 Pillars → 6 First Call → complete. T7's "first partner call completed" advance targets the NEW numbering (completing step 6), not the spec's old step 7.
- T4 already shipped the consecutive-no-show count and days-since-last-completed-call computations (SQL helpers behind the partner dashboard). T9 MUST consume those helpers, not reimplement them — one computation, two consumers (dashboard display + alerting).
- call_bookings now stores per-row ghl_location_id; all GHL calls go through per-row location. T7's webhook matching works on ghl_appointment_id as specced.
- The on-call dispatcher email channel is armed (ops alert email set in prod) — T9's alerts will actually reach a human.
- Call times now render through a shared member-timezone formatter — T8's reminder templates must use it (times in the member's zone with abbreviation).

T7 — GHL APPOINTMENT WEBHOOKS: Extend POST /webhooks/ghl (existing HMAC route) with an appointment-events branch: match ghl_appointment_id → call_bookings row → forward-only status transitions. completed → status=completed (advances onboarding to complete if it's the first partner call per the NEW 6-step numbering; feeds last-completed-call). no_show → status=no_show (feeds the consecutive counter; any completed call resets it). canceled → status=canceled (NOT a no-show; frees the day's cap slot if not already freed by our own cancel path — do not double-free). Unmatched appointment ids: log and ignore (other calendars exist in the location). Idempotent: replayed webhooks cannot regress a status or double-fire an advance. FLAGGED CONFIG (non-code, blocks final acceptance): the GHL location must be subscribed to appointment-status events pointing at /webhooks/ghl — report exactly what needs configuring so it can be done GHL-side; the task is not DONE-done until a real GHL status change round-trips into call_bookings, but code+tests can merge ahead of that.

T8 — CALL REMINDERS (email + SMS from the portal): Near-copy the scheduled-comms coaching pattern: 24h-before email + 1h-before SMS per booked call (kickoff and partner variants), same dedup (checkAndRecordSend). New per-category opt-in column partnerCallSmsOptIn following the existing pattern, enforced at the single send point. Canceled/rescheduled bookings must not fire stale reminders (recheck booking status at send time). Times in reminder copy use the shared member-timezone formatter. SMS is env-gated in prod (TWILIO_ACCOUNT_SID) — degrade to email-only gracefully and say so in the report.

T9 — ESCALATIONS + CAPACITY (through the on-call dispatcher, never ad-hoc email): A scheduled evaluator (reuse the scheduled-comms cadence infra) firing three alerts with fire/clear semantics + throttling via the existing dispatcher, each also landing in the System Health alert timeline per the registry pattern:
(a) 3 consecutive no-shows per member (from T4's helper; resets on completed) → "Mentee escalation: {name} — 3 consecutive no-shows".
(b) Vanish rule: active 3-Month+ term AND active assignment AND no completed partner call in 14 days (from last completed, or assignment start if never called) → distinct message.
(c) Fleet capacity: trailing-7-day booked-slots ÷ available-slots across ACTIVE partners ≥ 80% → "Partner capacity ≥80% — time to hire." Available slots = GHL free-slots snapshot bounded by each partner's max_daily_calls (currently 6). Note Myco is is_active=false — excluded from both numerator and denominator until armed.
Acceptance: simulated third consecutive no-show fires exactly one alert and clears on next completed call; a 14-day-dark member fires the vanish alert; capacity math verified against a constructed roster; all three visible in System Health.

CROSS-CUTTING (unchanged from spec): call_bookings is the single local truth; partner role grants no entitlements; every schema change ships idempotent companion SQL + drift baseline per repo convention.