CORRECTION to the in-flight hardening task — a read-only audit just completed and three findings change the spec. Adjust before finishing; if you've already built any of these parts the old way, rework them now rather than marking complete:

1. RECONCILIATION ALERTS — paid_reconciliation_needed is NOT a persisted order status (bts_orders' CHECK constraint doesn't include it; the only durable trace is the JSONB result in checkout_idempotency plus a console.error). So do NOT build anything that queries orders by that status — it will match nothing. Instead hook the alert at the exact points the outcome is produced in checkout-core.ts: (a) charge-succeeded-but-paid-flip-threw (~lines 319-333), (b) paid-but-onOrderPaid-threw (~lines 357-371), and (c) the grantPending path (~lines 388-401). Plus an hourly sweep over checkout_idempotency JSONB for reconciliation results (catches alert-queue failures).

2. CHARGER HEARTBEAT + DIGEST — write the charger's last_run_at to a small DB TABLE, not (only) Redis: the failure mode we're guarding is Redis itself dying, and a Redis-stored heartbeat dies with it. Also: the daily digest is the dead-man's-switch for the scheduler, so schedule it via a mechanism that does NOT share BullMQ/Redis's failure mode if feasible (e.g. a setInterval daily check in the web process with DB-guarded duplicate prevention). If full independence isn't feasible, do the closest thing and STATE THE RESIDUAL RISK in your report. Also include in the digest: subscriptions by status incl. past_due/unpaid with retry state (this doubles as the missing dunning report). And raise the renewal worker's "failed" events from console.error to a queued alert.

3. DROP Part 3 (the purge script) entirely — prod and dev have ZERO native-NMI billing rows (audited: 0 orders, 0 subscriptions, 0 payment_methods, 0 idempotency rows, 0 NMI-originated grants). There is nothing to purge. REPLACE it with a docs-only deliverable: docs/nmi-merchant-swap-checklist.md — (1) rotate exactly BTS_NMI_SECURITY_KEY + BTS_NMI_TOKENIZATION_KEY (the only two NMI credentials; both read only in nmi-gateway.ts); (2) REDEPLOY/RESTART required, because the tokenization key is served to the browser at runtime via GET /billing/tokenization-key — checkout keeps using the old public key until restart; (3) re-verify zero-row counts immediately before swap; (4) post-swap smoke test: one checkout + confirm the tokenization-key endpoint returns the new key.

4. One addition to Part 1: the existing middleware/rate-limiter.ts no-ops when Redis is absent — do NOT inherit that behavior. If Redis is unavailable, your billing limiter must fall back to a bounded in-memory per-process window (and log loudly), never silently stop limiting money endpoints.

Everything else in the original task stands as written. Acceptance criteria adjust accordingly: alerts fire from the three checkout-core hook points (test by forcing each), heartbeat lands in the DB, digest states its residual risk, no purge script, checklist doc exists.
