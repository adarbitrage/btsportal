Post-merge verification: AI Assistant consolidation (retire chat widget, repoint AI Assistant page onto the Live AI Documents backend). A merged task was supposed to deliver all of the following. Verify each item against the actual code and running app, fix anything missing or broken, and give me a pass/fail checklist at the end:

The /ai-assistant page now talks to the modern chat backend (/api/chat, Claude with streaming) — NOT the legacy /api/ai-chat route. Test it live: ask "who are the BTS coaches?" and confirm the answer comes from the curated "BTS Coaching Roster" doc (Sasha, Bruce, Todd) with no mention of Robin.
The legacy stack is fully deleted: the /api/ai-chat routes, the GPT-5 handler, the hardcoded getSystemPrompt() (the one containing "Robin (1-on-1 sessions)" and the KEY TOOLS list), and the dead 11-category list. Grep for "Robin" in the api-server source — the only acceptable hits are curated-content-related, never a prompt.
The floating chat widget no longer renders anywhere in the portal; the AI Assistant page is the only member chat surface. /chat still redirects to /ai-assistant.
Tier gating is removed from the chat backend: no daily-limit differences, no tier-gated features on the page; every entitled member gets the same experience. Session history sidebar works on the page; old legacy conversations are gone (fresh start).
The legacy AI-assistant conversation tables were retired via an idempotent drop wired into post-merge setup — confirm the drop actually ran in this environment.
The voice assistant prompt (buildVoiceSystemPrompt) no longer contains the hardcoded "BTS KEY TOOLS:" or "COACHING TEAM:" fact lines, and its chat-handoff wording points members to the AI Assistant page (not the retired widget). Confirm the updated prompt was pushed to the voice provider on boot.
The chat system prompt gained two new sentinel-enforced rules: (a) match answer depth to the question (overview docs for broad questions, lesson docs for specific ones), (b) synthesize overlapping retrieved docs as one body of truth. Confirm both sentinels are in the boot-enforcement check and the active DB prompt contains both rules.
All legacy privacy-scrub/name-leak guard tests were migrated to the surviving path and are green; typecheck and the test suite pass.
Data work that could NOT merge (do it now in this environment if missing): confirm the Live AI Documents corpus answers "what tools do I get with BTS?" and "who are the coaches?" with verified docs. If the tools-overview doc is missing, tell me — do not author content without my review.
For anything that failed, finish the work yourself, then re-verify. Note: this project's rule is that member-visible changes aren't done until published and canary-verified — after everything passes, remind me about publishing.