Trademark marking — append ™ to all brand names across the portal and all portal-sent communications. Architecture: mark at the BRAND CONFIG level so every substituted render inherits it automatically; sweep literal occurrences separately. Do NOT do a blind find-and-replace across the codebase.

1. BRAND CONFIG (the multiplier): In the brand config (@workspace/brand-config BRAND_TABLE), append ™ to the FULL display name of all seven brands: Build Test Scale™ (bts fallback), Your Second Engine™, The Backroad System™, The Off-Market Affiliate System™, The Reserve Income System™, The Silent Partner System™, Test Like Mad™. Rules: (a) ™ goes on the full form only — short forms (YSE, Backroad, BTS, TLM…) stay unmarked; (b) derived possessives must NOT inherit it mid-word — "Your Second Engine™'s" is wrong; possessive derivation applies to the unmarked name ("Your Second Engine's"), so store the mark separately or strip it before deriving. Every {{brand}} / brand.full render then shows ™ automatically across lessons, onboarding, reveal cards, the partner panel, and emails that use brand tokens.

2. LITERAL PORTAL SWEEP: Find every literal "Build Test Scale" in member-visible portal UI text (welcome/onboarding copy, footer, disclaimers, login/signup, dashboard chrome, the docx/legal display pages' headers if member-visible) and append ™ to the full phrase. EXCLUDE: code identifiers, slugs, URLs, env/config keys, DB values, alt-text-irrelevant assets, the curation map doc itself, and admin-only surfaces (admin panel text is optional — skip unless trivial). Where "Build Test Scale" appears as part of an already-marked feature name or the compound "BTS X" abbreviation forms, leave as-is.

3. OUTBOUND COMMS SWEEP: Same marking in all portal-SENT communications: transactional email templates, the reminder templates (24h/1h call reminders), the email sequences content stored in our system, and SMS templates — with one exception: if any SMS encoding constraint makes ™ render as garbage or split segments (™ is non-GSM-7; it forces UCS-2 encoding and halves segment length), use "(TM)" in SMS or omit there, and report which you did. Subject lines included where the name appears.

4. REPORT: file-level list of literal replacements, confirmation the config-level change propagates (show one substituted render before/after), the possessive-derivation test passing, and anything you deliberately left unmarked with the reason.