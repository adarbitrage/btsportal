BUILD: Tipalti Phase 1 — hosted iframe onboarding + payable status (no money moves; safe to ship first)
Build the onboarding half of the Tipalti payout integration. This phase moves no money — it makes members able to be paid and finally uses the dead users.tipalti_onboarded column. Recon (prior task) confirmed the API surface; build against it. Phase 2 (payout sending) is deferred pending external confirmations — do NOT build payout submission in this task.
Naming rule (applies everywhere member-facing): the affiliate network is branded "Media Mavens" to members — a white-label of Tapfiliate. "Tapfiliate" must NEVER appear in any member-facing surface (UI, copy, the payout-setup page, error messages, the support agent). Internal code/variables can keep Tapfiliate names; but anything a member sees says "Media Mavens." Note: Tipalti is the payout processor — Tipalti's own iframe will show its own branding (that's fine, members do log into Tipalti for bank setup, per the existing copy).
Build:

Tipalti API client (server/services/tipalti-client.ts) — model on tapfiliate-client.ts (class around fetch, reads secrets from process.env in constructor, NOT the Replit-connector pattern). Reads TIPALTI_PAYER_NAME, TIPALTI_API_KEY, TIPALTI_ENV (sandbox/prod), iframe base URL. Fail-closed if unset.
Signed iframe URL generation — server-side, on demand, per page load (the ts is valid only 1 minute). Build the auth string PayerName + idap + ts (idap = our member id), UTF-8 encode, HMAC-SHA256 with the private API key, hex digest = hashkey. Parameter order is significant. Use the correct base URL for sandbox vs. prod (ui2.tipalti.com / ui2.sandbox.tipalti.com).
IPN receiver for PayeeStatusChanged — a public webhook route (mount pattern from webhooks-ghl.ts, fail-closed). ⚠️ Tipalti IPN is NOT cleanly HMAC-signed like our other webhooks — it posts URL-encoded body, and authenticity is via secret-in-URL / IP allow-list / call-back confirm. Implement the verification mechanism appropriate to our account's IPN setup; do NOT assume HMAC. Flag in your halt exactly what verification you implemented and confirm it's adequate (this governs a money-adjacent event, so it must be real). On a verified PayeeStatusChanged → Payable, flip the member's payable status.
Schema (net-new, manual Neon SQL migration): a payee-id/idap mapping for the member, and a richer payable status than the single dead boolean (the tipalti_onboarded column exists but is never read/written — wire it, or replace it with a proper status field). Model on shared/models/*. Do NOT build the payout-records table yet (that's Phase 2).
Frontend payout-setup page (client/src/pages/dashboard/) — mirror the earnings/how-you-get-paid page pattern, wouter route in App.tsx (lazy, useAuth-gated), server-gated by isAuthenticated. Embeds the server-signed Tipalti iframe so the member completes bank/tax setup directly in Tipalti. Nav item for logged-in members. Member-facing copy says "Media Mavens" for the affiliate network; the bank-setup step correctly references Tipalti (the processor).

HALT with: the signed-URL generation (confirm HMAC + 1-min ts + param order), the IPN receiver + exactly what authenticity verification was implemented (since it's not standard HMAC), the schema + migration, the gated frontend page, confirmation no "Tapfiliate" string appears member-facing, and that NO payout-sending code was built (Phase 1 only). Architect review + commit. Built-but-dormant until TIPALTI_* secrets are set — that's expected.