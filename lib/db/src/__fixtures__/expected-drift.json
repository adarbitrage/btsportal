{
  "constraints": {
    "onlyInPush": [
      "blitz_daily_activity | f | FOREIGN KEY (user_id) REFERENCES users(id)",
      "blitz_daily_activity | p | PRIMARY KEY (user_id, activity_date)",
      "blitz_events | f | FOREIGN KEY (user_id) REFERENCES users(id)",
      "blitz_events | p | PRIMARY KEY (id)",
      "blitz_phases | p | PRIMARY KEY (slug)",
      "coach_google_connections | f | FOREIGN KEY (user_id) REFERENCES users(id)",
      "coach_google_connections | p | PRIMARY KEY (id)",
      "coach_google_connections | u | UNIQUE (user_id)"
    ],
    "onlyInMigrations": []
  },
  "indexes": {
    "onlyInPush": [
      "blitz_daily_activity | CREATE UNIQUE INDEX <name> ON public.blitz_daily_activity USING btree (user_id, activity_date)",
      "blitz_events | CREATE INDEX <name> ON public.blitz_events USING btree (user_id, course_id, occurred_at)",
      "blitz_events | CREATE INDEX <name> ON public.blitz_events USING btree (user_id, occurred_at)",
      "blitz_events | CREATE UNIQUE INDEX <name> ON public.blitz_events USING btree (id)",
      "blitz_phases | CREATE UNIQUE INDEX <name> ON public.blitz_phases USING btree (slug)",
      "coach_google_connections | CREATE UNIQUE INDEX <name> ON public.coach_google_connections USING btree (id)",
      "coach_google_connections | CREATE UNIQUE INDEX <name> ON public.coach_google_connections USING btree (user_id)"
    ],
    "onlyInMigrations": []
  }
}
